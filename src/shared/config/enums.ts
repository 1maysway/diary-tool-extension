export enum APP_STORAGE_PATHS {
  settings = 'settings',
  logins = 'logins',
  stands = 'stands',
  selectedData = 'selectedData',
  hasEnteredLogin = 'hasEnteredLogin',
  visualActionProps = 'visualActionProps',
}

export enum COOKIE_KEYS {
  lastCallbackUrl = 'lastCallbackUrl',
}
